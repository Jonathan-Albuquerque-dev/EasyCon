import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {},

  input: {
    backgroundColor: "#DEDEDE",
    width: "80%",
    height: 43,
    borderRadius: 5,
    marginBottom: 10,
    padding: 10,
  },
});
